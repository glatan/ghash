[package]
name = "sha1"
version = "0.1.0"
authors = ["glatan <glatan@protonmail.com>"]
edition = "2018"

[dependencies]
utils = { path = "../../utils" }

[dev-dependencies]
dev-utils = { path = "../../dev-utils" }
