[package]
name = "ripemd"
version = "0.1.0"
authors = ["glatan <glatan@protonmail.com>"]
edition = "2018"
build = "../../../build.rs"

[features]
minimal = []

[dependencies]
utils = { path = "../../utils" }

[dev-dependencies]
dev-utils = { path = "../../dev-utils" }
